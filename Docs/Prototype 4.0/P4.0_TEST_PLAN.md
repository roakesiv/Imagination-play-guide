# P4.0 Test Plan — Architecture Runway

**Project:** Magic Creature Card Maker / Imagination Play Guide  
**Prototype:** P4.0  
**Test focus:** Refactor safety, prompt-output contracts, and behavior preservation

---

## 1. Test Goal

Validate that V4.0 architecture changes preserve current app behavior while making prompt outputs easier to maintain and extend.

---

## 2. Browser Smoke Test

| Step | Expected Result | Pass/Notes |
|---|---|---|
| Open app | App loads |  |
| Parent Tips open/collapse | Tips work |  |
| Fill Example | Fields populate |  |
| Make Creature | Image prompt, summary, details appear |  |
| Copy Prompt | Prompt copies or fallback selects |  |
| Select Creature Card | Output appears and uses creature data |  |
| Select Story | Output appears and uses creature data |  |
| Select Adventure | Output appears and uses creature data |  |
| Select Coloring Page | Output appears and uses creature data |  |
| Select Find-It Game | Output appears and uses creature data |  |
| Copy bridge output | Output copies or fallback selects |  |
| Reset | Fields and outputs clear |  |

---

## 3. Behavior Preservation Test

| Behavior | Expected Result | Pass/Notes |
|---|---|---|
| App remains static | No build step required |  |
| GitHub Pages compatible | Works when published |  |
| `content.js` still renders fields | UI copy appears correctly |  |
| Existing field ids still work | Form reads all values |  |
| Missing fields | Render as `not specified` |  |
| Fill Example | Uses current example values |  |
| Parent Tips | Still visible and compact |  |
| Button colors | Still distinct |  |
| Header readability | Still acceptable |  |

---

## 4. Creature Data Contract Test

Expected creature data keys:

| Key | Present? | Notes |
|---|---|---|
| `name` | Yes | Explicit in `creatureDataKeys`; maps from `creatureName`. |
| `creatureMix` | Yes | Explicit in `creatureDataKeys`. |
| `magic` | Yes | Explicit in `creatureDataKeys`. |
| `colors` | Yes | Explicit in `creatureDataKeys`. |
| `home` | Yes | Explicit in `creatureDataKeys`. |
| `personality` | Yes | Explicit in `creatureDataKeys`. |
| `accessories` | Yes | Explicit in `creatureDataKeys`. |
| `extraDetail` | Yes | Explicit in `creatureDataKeys`. |
| `pictureStyle` | Yes | Explicit in `creatureDataKeys`. |

Expected result:

- Template code consumes the creature data object.
- Templates do not directly query DOM fields.

---

## 5. Prompt Template Matrix

Use this matrix after WP2-WP4 architecture changes and before V4.1 prompt-quality work. V4.0 should verify coverage and behavior preservation, not improve prompt quality.

| Output Type | Template Location | Required Data Checks | Required Guardrails / Instructions | Missing Field Check | Copy Check | Status / Notes |
|---|---|---|---|---|---|---|
| Image Prompt / Creature Profile Card | `promptTemplates.js` `buildImagePrompt` | Uses name, creature mix, magic, colors, home, personality, accessories, extra detail, picture style | Kid-friendly, cute/colorful/whimsical, preserve silly details, avoid scary/violent/horror/adult, readable profile-card image | Blank fields render as `not specified` | Copy Prompt copies or fallback selects | Browser smoke tested. |
| Creature Card | `promptTemplates.js` `artifacts.card` | Uses name, creature mix, magic, colors, home, personality, accessories, extra detail | Kid-friendly magical creature profile card; preserve specific silly detail | Blank fields render as `not specified` | Bridge Copy copies or fallback selects | Browser smoke tested. |
| Story | `promptTemplates.js` `artifacts.story` | Uses name, creature mix, magic, home, colors, personality, extra detail | Short, cozy, kid-friendly; safe/happy ending | Blank fields render as `not specified`; blank name uses `the creature` where grammar needs it | Bridge Copy copies or fallback selects | Browser smoke tested. |
| Adventure | `promptTemplates.js` `artifacts.adventure` | Uses name, creature mix, home, magic, personality, accessories, colors | Gentle adventure seed for young kids; friendly helper; silly/non-scary obstacle; cheerful reward | Blank fields render as `not specified`; blank name uses `the creature` where grammar needs it | Bridge Copy copies or fallback selects | Browser smoke tested. |
| Coloring Page | `promptTemplates.js` `artifacts.coloring` | Uses name, creature mix, magic, home, personality, extra detail | Kid-friendly printable coloring page; clean bold outlines; simple shapes; no heavy shading; no scary details | Blank fields render as `not specified` | Bridge Copy copies or fallback selects | Browser smoke tested. |
| Find-It Game | `promptTemplates.js` `artifacts.findIt` | Uses name, creature mix, home, magic, colors, personality, derived find-it items from accessories/extra detail/magic/colors/home | Kid-friendly hidden-object activity; whimsical/readable/safe; full of fun details | Blank fields render as `not specified`; default hidden objects appear if no derived items exist | Bridge Copy copies or fallback selects | Browser smoke tested. |

### Output-Specific Regression Checks

| Check | Expected Result | Pass/Notes |
|---|---|---|
| Image prompt includes all nine creature data fields | Prompt has Name, Creature mix, Magic, Colors, Home, What they are like, Accessories, Extra weird detail, Picture style |  |
| Bridge outputs use current form values after editing fields | Selected bridge output updates from current creature data |  |
| Already-selected bridge output refreshes after Make Creature | Existing selected output rerenders with current creature data |  |
| Unknown artifact type is ignored safely | No crash if an unknown output type is requested |  |
| Find-It derived items split comma-separated accessories/details | Things to find list includes separate trimmed items |  |
| Find-It fallback items appear when no fields produce items | Things to find list uses default hidden objects |  |

---

## 6. WP5.5 Architecture Hardening Checks

| Check | Expected Result | Pass/Notes |
|---|---|---|
| Missing-field fallback source | `not specified` comes from one shared builder helper |  |
| Image prompt builder availability | Missing image template fails gracefully with readable fallback text |  |
| Unknown artifact type | Builder returns `null`; UI does not crash |  |
| Bridge icons | Choice icons render as intended, with no mojibake |  |
| No output type changes | Existing bridge output choices remain unchanged |  |

---

## 7. Missing Field Test

| Scenario | Expected Result | Pass/Notes |
|---|---|---|
| All fields blank | No crash; outputs use `not specified` |  |
| Name blank | Output still works |  |
| Home blank | Output still works |  |
| Picture style blank | Output still works |  |
| Extra detail blank | Output still works |  |

---

## 8. Architecture Review Checklist

| Question | Result / Notes |
|---|---|
| Are prompt templates easy to find? |  |
| Is UI copy still in `content.js`? |  |
| Is prompt engineering text separate from UI copy? |  |
| Is creature data shape explicit? |  |
| Does script/UI code avoid owning template text? |  |
| Can a new output be added with minimal code changes? |  |
| Did implementation avoid new dependencies? |  |
| Did implementation avoid adding new product features? |  |

---

## 9. Mobile Sanity Test

Even though V4.0 is internal, run a quick mobile test after publish.

| Check | Pass/Notes |
|---|---|
| App loads on phone |  |
| Make Creature works |  |
| Copy Prompt works |  |
| Bridge output works |  |
| Reset works |  |
| No visible UX regression |  |

---

## 10. V4.0 Validation Decision

| Decision Question | Answer |
|---|---|
| Did architecture improve maintainability? |  |
| Did user-facing behavior remain stable? |  |
| Is the app ready for V4.1 Coloring + Find-It prompt quality? |  |
| Are any blocker fixes needed before V4.1? |  |
